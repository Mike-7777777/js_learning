// packages, modules, dependency

// use npm

const express = require('express')

// package.json stores metadata(proj name/version/description/author/repository/license), dependencies, scipts


// 'npm init -y' can init a project and create a package file

// 'npm install <package name>' to download and add package to dependencies
// 'npm install --save-dev <package name>' to download and add package to devDependencies 
